#header-left {
    background-color: #065A82; /* or the original value if different */
    border: 1px solid #9EB3C2;
}

#atlas-logo {
    /* keep default scaling via logic.py */
}

#atlas-title {
    color: #FFFFFF;
    font-family: Montserrat;
    font-weight: bold;
    font-size: 17px;
    background: transparent;
}
